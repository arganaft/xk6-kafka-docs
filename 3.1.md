# Альтернатива git clone — скачивание архива через curl
RUN curl -fsSL -o /tmp/rw.tar.gz \
    "https://token:1234567890@sberosc.sigma.sbrf.ru/repo/github-raw/risingwavelabs/risingwave/archive/refs/tags/${RISINGWAVE_VERSION}.tar.gz" && \
    tar -xzf /tmp/rw.tar.gz --strip-components=1 -C /build && \
    rm /tmp/rw.tar.gz
