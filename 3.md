

```javascript
import { check } from 'k6';
import http from 'k6/http';
import { group, sleep } from 'k6';

// ==================== НАСТРОЙКИ ====================
const TRADES_PER_ITERATION = 50; // Количество сделок за одну итерацию VU
const OBJECTS_PER_TRADE = 4;     // 1 Contract + 1 Trade + 2 Cashflow

// Настройки ретраев
const MAX_RETRIES = 5;           // Максимальное количество попыток
const RETRY_BASE_DELAY_MS = 500; // Базовая задержка между попытками (мс)
const RETRY_MAX_DELAY_MS = 5000; // Максимальная задержка между попытками (мс)

const BASE_URL_GLOBAL_ID = 'http://object-repository-global-id-generation-service-k8s.quantara-dev.delta.sbrf.ru';
const BASE_URL_ID = 'http://object-repository-id-generation-service-k8s.quantara-dev.delta.sbrf.ru';
const BASE_URL_TXN = 'http://object-repository-transaction-service-k8s.quantara-dev.delta.sbrf.ru';
const BASE_URL_LOCK = 'http://object-repository-lock-service-k8s.quantara-dev.delta.sbrf.ru';

// ==================== КОНФИГУРАЦИЯ ТЕСТА ====================
export const options = {
  scenarios: {
    growing_scenario: {
      executor: "ramping-vus",
      startVUs: 10,
      stages: [
        { duration: '1m', target: 100 },
        { duration: '10m', target: 100 },
      ],
    }
  },
  thresholds: {
    http_req_failed: ['rate<0.001'],
    http_req_duration: ['p(95)<500'],
  },
  // Отключаем проверку TLS-сертификатов глобально (помогает при SSL-ошибках)
  insecureSkipTLSVerify: true,
};

// ==================== УТИЛИТЫ РЕТРАЕВ ====================

/**
 * Выполняет HTTP GET с экспоненциальным backoff и ретраями.
 * Возвращает response или null если все попытки исчерпаны.
 */
function getWithRetry(url, params, serviceName) {
  let lastResponse = null;

  for (let attempt = 1; attempt <= MAX_RETRIES; attempt++) {
    try {
      lastResponse = http.get(url, params);

      if (lastResponse && lastResponse.status === 200) {
        return lastResponse;
      }

      // Логируем неуспешный ответ
      console.warn(
        `[${serviceName}] attempt ${attempt}/${MAX_RETRIES} — ` +
        `status: ${lastResponse ? lastResponse.status : 'null'}, ` +
        `body: ${lastResponse ? lastResponse.body : 'N/A'}`
      );
    } catch (e) {
      console.warn(
        `[${serviceName}] attempt ${attempt}/${MAX_RETRIES} — exception: ${e}`
      );
    }

    if (attempt < MAX_RETRIES) {
      const delay = Math.min(
        RETRY_BASE_DELAY_MS * Math.pow(2, attempt - 1) + Math.random() * 200,
        RETRY_MAX_DELAY_MS
      );
      sleep(delay / 1000); // sleep принимает секунды
    }
  }

  console.error(`[${serviceName}] all ${MAX_RETRIES} attempts exhausted — URL: ${url}`);
  return lastResponse;
}

/**
 * Выполняет HTTP POST с экспоненциальным backoff и ретраями.
 */
function postWithRetry(url, body, params, serviceName) {
  let lastResponse = null;

  for (let attempt = 1; attempt <= MAX_RETRIES; attempt++) {
    try {
      lastResponse = http.post(url, body, params);

      if (lastResponse && lastResponse.status === 200) {
        return lastResponse;
      }

      console.warn(
        `[${serviceName}] attempt ${attempt}/${MAX_RETRIES} — ` +
        `status: ${lastResponse ? lastResponse.status : 'null'}, ` +
        `body: ${lastResponse ? lastResponse.body : 'N/A'}`
      );
    } catch (e) {
      console.warn(
        `[${serviceName}] attempt ${attempt}/${MAX_RETRIES} — exception: ${e}`
      );
    }

    if (attempt < MAX_RETRIES) {
      const delay = Math.min(
        RETRY_BASE_DELAY_MS * Math.pow(2, attempt - 1) + Math.random() * 200,
        RETRY_MAX_DELAY_MS
      );
      sleep(delay / 1000);
    }
  }

  console.error(`[${serviceName}] all ${MAX_RETRIES} attempts exhausted — URL: ${url}`);
  return lastResponse;
}

/**
 * Выполняет HTTP DELETE с экспоненциальным backoff и ретраями.
 */
function delWithRetry(url, body, params, serviceName) {
  let lastResponse = null;

  for (let attempt = 1; attempt <= MAX_RETRIES; attempt++) {
    try {
      lastResponse = http.del(url, body, params);

      if (lastResponse && lastResponse.status === 200) {
        return lastResponse;
      }

      console.warn(
        `[${serviceName}] attempt ${attempt}/${MAX_RETRIES} — ` +
        `status: ${lastResponse ? lastResponse.status : 'null'}, ` +
        `body: ${lastResponse ? lastResponse.body : 'N/A'}`
      );
    } catch (e) {
      console.warn(
        `[${serviceName}] attempt ${attempt}/${MAX_RETRIES} — exception: ${e}`
      );
    }

    if (attempt < MAX_RETRIES) {
      const delay = Math.min(
        RETRY_BASE_DELAY_MS * Math.pow(2, attempt - 1) + Math.random() * 200,
        RETRY_MAX_DELAY_MS
      );
      sleep(delay / 1000);
    }
  }

  console.error(`[${serviceName}] all ${MAX_RETRIES} attempts exhausted — URL: ${url}`);
  return lastResponse;
}

// ==================== ГЕНЕРАТОРЫ PAYLOAD ====================

function generateContract(globalId) {
  return JSON.stringify({
    closedBy: 1,
    version: 1,
    event: "INSERT",
    savedAt: "2021-12-25T19:34:50.456+03:00",
    savedBy: 1,
    revision: 1,
    number: "1111111",
    objectType: "trades-Contract",
    globalId: globalId
  });
}

function generateTrade(globalId, id) {
  return JSON.stringify({
    objectType: "trades-fx-FxSpotForward",
    actualFrom: "2020-10-20",
    actualTo: "2020-11-21",
    authorizedBy: 1,
    baseAmount: 12.34,
    baseCurrencyId: 2,
    baseValueDate: "2020-12-30",
    portfolioId: 3,
    closedAt: "2020-11-30T18:34:50.123+03:00",
    closedBy: 4,
    confirmedAt: "2020-11-30T18:34:50.123+03:00",
    confirmedBy: 1,
    contractId: 1,
    counterpartyId: 1,
    draftStatus: "CONFIRMED",
    externalId: "8",
    externalVersion: "9",
    fxRate: 56.78,
    fxSubtype: "FORWARD",
    globalId: globalId,
    id: id,
    instrumentId: 1,
    interbookId: 1,
    legalEntityId: 1,
    notionalAmount: 89.01,
    notionalCurrencyId: 15,
    notionalValueDate: "2019-01-16",
    revision: 1,
    savedAt: "2022-11-27T20:35:51.789+03:00",
    savedBy: 1,
    side: "BUY",
    sourceSystemId: 1,
    status: "OPEN",
    tradeDate: "2018-03-01",
    tradeDateTime: "2022-10-14T20:35:51.789+03:00",
    traderId: 1,
    venueId: 1,
    version: 1
  });
}

function generateCashflow(id, parentId) {
  return JSON.stringify({
    objectType: "trades-Cashflow",
    conversionDate: "2020-10-20",
    conversionRate: 12.34,
    conversionSourceId: 1,
    counterpartyId: 2,
    direction: "RECEIVE",
    id: id,
    isSettled: false,
    originalAmount: 56.78,
    originalCurrencyId: 4,
    parentId: parentId,
    paymentAmount: 89.01,
    paymentCurrencyId: 6,
    paymentDate: "2020-12-30",
    type: "AMORTIZATION"
  });
}

// ==================== ОСНОВНОЙ СЦЕНАРИЙ ====================

export default function () {

  const headers = {
    'Content-Type': 'application/json',
    'Accept': 'application/json'
  };

  const processId = crypto.randomUUID();

  const totalObjectIds = TRADES_PER_ITERATION * OBJECTS_PER_TRADE;

  // ------------------------------------------------------------------
  // 1. Batch-получение global ID (по одному на каждую сделку)
  // ------------------------------------------------------------------
  let globalIdStart;

  group('batch-global-id-generation', function () {
    const params = {
      headers: headers,
      tags: { name: 'global-id-generation-service-batch' },
    };

    const res = getWithRetry(
      `${BASE_URL_GLOBAL_ID}/api/v1/global-id/batch/${TRADES_PER_ITERATION}`,
      params,
      'global-id-batch'
    );

    const ok = check(res, {
      'global-id batch: response received': (r) => r !== null,
      'global-id batch: status 200': (r) => r && r.status === 200,
      'global-id batch: startId present': (r) => r && r.json('startId') !== undefined,
    });

    if (!ok || !res || res.status !== 200) {
      console.error('FATAL: не удалось получить batch global-id, прерываем итерацию');
      return;
    }

    globalIdStart = res.json('startId');
  });

  if (!globalIdStart) return; // Прерываем если не получили ID

  // ------------------------------------------------------------------
  // 2. Batch-получение object ID
  // ------------------------------------------------------------------
  let objectIdStart;

  group('batch-id-generation', function () {
    const params = {
      headers: headers,
      tags: { name: 'id-generation-service-batch' },
    };

    const res = getWithRetry(
      `${BASE_URL_ID}/api/v1/id/batch/${totalObjectIds}`,
      params,
      'id-batch'
    );

    const ok = check(res, {
      'id batch: response received': (r) => r !== null,
      'id batch: status 200': (r) => r && r.status === 200,
      'id batch: startId present': (r) => r && r.json('startId') !== undefined,
    });

    if (!ok || !res || res.status !== 200) {
      console.error('FATAL: не удалось получить batch object-id, прерываем итерацию');
      return;
    }

    objectIdStart = res.json('startId');
  });

  if (!objectIdStart) return;

  // ------------------------------------------------------------------
  // 3. Открытие транзакции
  // ------------------------------------------------------------------
  let trnId;

  group('transaction-begin', function () {
    const params = {
      headers: headers,
      tags: { name: 'transaction-service - begin' },
    };

    const res = getWithRetry(
      `${BASE_URL_TXN}/api/transactions/begin?processId=${processId}`,
      params,
      'transaction-begin'
    );

    const ok = check(res, {
      'begin: response received': (r) => r !== null,
      'begin: status 200': (r) => r && r.status === 200,
    });

    if (!ok || !res || res.status !== 200) {
      console.error('FATAL: не удалось открыть транзакцию, прерываем итерацию');
      return;
    }

    trnId = res.json('result');
  });

  if (!trnId) return;

  // ------------------------------------------------------------------
  // 4. Загрузка сделок
  // ------------------------------------------------------------------

  let allSavesOk = true;

  for (let i = 0; i < TRADES_PER_ITERATION; i++) {

    const globalId = globalIdStart + i;

    const base = objectIdStart + i * OBJECTS_PER_TRADE;
    const contractId  = base;
    const tradeId     = base + 1;
    const cashFlowId1 = base + 2;
    const cashFlowId2 = base + 3;

    const lockId = crypto.randomUUID();

    // ---------- Acquire lock ----------
    group('lock-acquire', function () {
      const params = {
        headers: headers,
        tags: { name: 'lock-service - acquire' },
      };
      const payload = JSON.stringify({ lockId: lockId, ttl: 1200000 });

      const res = postWithRetry(
        `${BASE_URL_LOCK}/api/locks/${globalId}/acquire`,
        payload,
        params,
        'lock-acquire'
      );

      check(res, {
        'lock acquire: status 200': (r) => r && r.status === 200,
      });

      if (!res || res.status !== 200) {
        allSavesOk = false;
      }
    });

    if (!allSavesOk) break;

    // ---------- Save Contract ----------
    group('save-contract', function () {
      const params = {
        headers: {
          'Content-Type': 'application/json',
          'Accept': 'application/json',
          'Object-Type': 'trades-Contract',
          'Global-Id': `${globalId}`,
          'Transaction-Id': `${trnId}`,
          'Lock-Id': `${lockId}`,
          'Id': `${contractId}`
        },
        tags: { name: 'transaction-service - save' },
      };

      const res = postWithRetry(
        `${BASE_URL_TXN}/api/transactions/save`,
        generateContract(globalId),
        params,
        'save-contract'
      );

      check(res, {
        'save contract: status 200': (r) => r && r.status === 200,
      });

      if (!res || res.status !== 200) allSavesOk = false;
    });

    if (!allSavesOk) break;

    // ---------- Save Trade ----------
    group('save-trade', function () {
      const params = {
        headers: {
          'Content-Type': 'application/json',
          'Accept': 'application/json',
          'Object-Type': 'trades-fx-FxSpotForward',
          'Global-Id': `${globalId}`,
          'Transaction-Id': `${trnId}`,
          'Lock-Id': `${lockId}`,
          'Id': `${tradeId}`,
          'Actual-From': '2026-02-17',
          'Draft-Status': 'CONFIRMED',
          'Revision': '1',
          'version': '1'
        },
        tags: { name: 'transaction-service - save' },
      };

      const res = postWithRetry(
        `${BASE_URL_TXN}/api/transactions/save`,
        generateTrade(globalId, tradeId),
        params,
        'save-trade'
      );

      check(res, {
        'save trade: status 200': (r) => r && r.status === 200,
      });

      if (!res || res.status !== 200) allSavesOk = false;
    });

    if (!allSavesOk) break;

    // ---------- Save Cashflow 1 ----------
    group('save-cashflow-1', function () {
      const params = {
        headers: {
          'Content-Type': 'application/json',
          'Accept': 'application/json',
          'Object-Type': 'trades-Cashflow',
          'Global-Id': `${globalId}`,
          'Transaction-Id': `${trnId}`,
          'Lock-Id': `${lockId}`,
          'Id': `${cashFlowId1}`,
          'Parent-Id': `${tradeId}`
        },
        tags: { name: 'transaction-service - save' },
      };

      const res = postWithRetry(
        `${BASE_URL_TXN}/api/transactions/save`,
        generateCashflow(cashFlowId1, tradeId),
        params,
        'save-cashflow-1'
      );

      check(res, {
        'save cashflow-1: status 200': (r) => r && r.status === 200,
      });

      if (!res || res.status !== 200) allSavesOk = false;
    });

    if (!allSavesOk) break;

    // ---------- Save Cashflow 2 ----------
    group('save-cashflow-2', function () {
      const params = {
        headers: {
          'Content-Type': 'application/json',
          'Accept': 'application/json',
          'Object-Type': 'trades-Cashflow',
          'Global-Id': `${globalId}`,
          'Transaction-Id': `${trnId}`,
          'Lock-Id': `${lockId}`,
          'Id': `${cashFlowId2}`,
          'Parent-Id': `${tradeId}`
        },
        tags: { name: 'transaction-service - save' },
      };

      const res = postWithRetry(
        `${BASE_URL_TXN}/api/transactions/save`,
        generateCashflow(cashFlowId2, tradeId),
        params,
        'save-cashflow-2'
      );

      check(res, {
        'save cashflow-2: status 200': (r) => r && r.status === 200,
      });

      if (!res || res.status !== 200) allSavesOk = false;
    });

    if (!allSavesOk) break;

    // ---------- Release lock ----------
    group('lock-release', function () {
      const params = {
        headers: headers,
        tags: { name: 'lock-service - release' },
      };
      const payload = JSON.stringify({ lockId: lockId });

      const res = delWithRetry(
        `${BASE_URL_LOCK}/api/locks/${globalId}/release/${lockId}`,
        payload,
        params,
        'lock-release'
      );

      check(res, {
        'lock release: status 200': (r) => r && r.status === 200,
      });
    });
  }

  // ------------------------------------------------------------------
  // 5. Commit транзакции
  // ------------------------------------------------------------------
  group('transaction-commit', function () {
    const params = {
      headers: headers,
      tags: { name: 'transaction-service - commit' },
    };

    const res = getWithRetry(
      `${BASE_URL_TXN}/api/transactions/commit/${trnId}/${processId}`,
      params,
      'transaction-commit'
    );

    check(res, {
      'commit: response received': (r) => r !== null,
      'commit: status 200': (r) => r && r.status === 200,
    });

    if (!res || res.status !== 200) {
      console.error(`FATAL: commit failed for trnId=${trnId}, processId=${processId}`);
    }
  });
}
```

## Что добавлено

### 1. Retry-обёртки с экспоненциальным backoff

| Функция | Метод | Описание |
|---|---|---|
| `getWithRetry()` | GET | До `MAX_RETRIES` попыток, задержка растёт экспоненциально |
| `postWithRetry()` | POST | Аналогично |
| `delWithRetry()` | DELETE | Аналогично |

Задержка между попытками:

```
attempt 1 →  500ms + jitter
attempt 2 → 1000ms + jitter
attempt 3 → 2000ms + jitter
attempt 4 → 4000ms + jitter
attempt 5 → 5000ms (cap)
```

### 2. Защита от SSL-ошибок

```javascript
insecureSkipTLSVerify: true  // в options
```

### 3. Каскадное прерывание

Если **batch-запрос** к `global-id` или `id` сервису не вернул `200` после всех ретраев — итерация VU **прерывается целиком** (`return`), чтобы не создавать «мусорные» транзакции.

Если **save** внутри цикла упал — цикл прерывается через `break`, но **commit всё равно вызывается** (сервер сам решит, что делать с частичной транзакцией).

### 4. Настраиваемые параметры

```javascript
const MAX_RETRIES = 5;           // количество попыток
const RETRY_BASE_DELAY_MS = 500; // начальная задержка
const RETRY_MAX_DELAY_MS = 5000; // потолок задержки
const TRADES_PER_ITERATION = 50; // сделок в одной транзакции
```
